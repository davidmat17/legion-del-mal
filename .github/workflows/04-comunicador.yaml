name: Comunicador de la legion

on:
  push:
    workflow_dispatch:
      inputs: 
        villano: 
          description: "Que villano envia el mensaje"
          required: true
          type: choice
          options: 
            -lex luthor
            -magneto
            -joker
            -Mystique
            -Brainiac

jobs:
  build:
    transmitir-mensaje: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

     